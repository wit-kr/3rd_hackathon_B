import { Container } from '@/components/layout/mainLayout/styles';

// eslint-disable-next-line react/prop-types
const Layout = ({ children }) => <Container>{children}</Container>;

export default Layout;
