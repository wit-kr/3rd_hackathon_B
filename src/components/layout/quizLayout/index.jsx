import { Container } from '@/components/layout/quizLayout/styles';

// eslint-disable-next-line react/prop-types
const QuizLayout = ({ children }) => <Container>{children}</Container>;

export default QuizLayout;
